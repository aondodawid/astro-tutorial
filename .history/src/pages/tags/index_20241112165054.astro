---
import BaseLayout from "../../layouts/BaseLayout.astro";
const pageTitle = "Tag Index";
const tags = ['astro', 'blogging', 'learning in public', 'successes', 'setbacks', 'community']

---

<BaseLayout pageTitle={pageTitle}>
  <ul>
  allPosts.map((post) => <li></li>/>)

  </ul>
</BaseLayout>
